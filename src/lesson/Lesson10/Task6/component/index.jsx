import React, { useState } from 'react';

const PRODUCTS = ['Orange', 'Apple', 'Banana', 'Cherry'];

const Lesson = () => {
  const [products, setProducts] = useState([...PRODUCTS])
  return (
    <div>

    </div>
  );
};

export default Lesson;
